# stable-0.6.9

其他 :
- 文件	变更 AndroidDownloadStorageProvider.kt	listChildren/queryDisplayName 对 SecurityException/IllegalArgumentException 降级不冒泡；selectedTreeUri 校验持久化授权，失效视同未选择 AndroidBackupStorageProvider.kt	同样加固 + setSelectedFolder 授权无法持久化时拒绝接受（不再残留失效 URI） DownloadRepositoryImpl.kt	markThreadUpdateAvailable 增加 isReady() 守卫 AndroidDownloadStorageProviderTest.kt	新增 isTransientStorageFailure 用例
